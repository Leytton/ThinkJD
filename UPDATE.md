# V1.4.1_8升级日志
 1、增加class或bean自动获取表字段、数据和注解功能
 
 2、增加setInc、setDec函数用于数据库字段自增或自减
 
 3、增加asc、desc排序函数
 
 4、添加fetchSql=true调试模式中输出占位参数功能
 
 5、拆分field()函数为field()和data()，前者参数为","拼接的字段名字符串，后者参数为数据
